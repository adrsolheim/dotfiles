# .bash_profile

# Get the aliases and functions
if [ -f ~/.bashrc ]; then
	. ~/.bashrc
fi

# User specific environment and startup programs
export PATH=~/.npm-global:$PATH
export PATH=/opt/jetbrains-toolbox-1.18.7455:$PATH
